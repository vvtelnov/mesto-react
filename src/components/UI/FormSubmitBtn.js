function FormSubmitBtn(props) {
  return (
	  <button
      name="publication-create-button"
      type="submit"
      className="popup__save-button"
    >
      Создать
    </button>

  )
}

export default FormSubmitBtn;